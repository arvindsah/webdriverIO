0. Open the the git command line at the location where you want to set-up the project
//DONE use this one - 1. git init // to start the repo in particular location
2. git close URL // clone the repo
3. cd projectFolderName // imp
4. git remote -v // view all the acitve connevtions
something else

//just see


=>adding the url for private repository
git remote set-url origin https://YOUR_GITHUB_USER@github.com/aceofwings/RotairERP.git


=> Updating the Comitter name and emaild addess
 git config --list// to check the name and email
 git config --global user.name "Arvind Sah"
 git config --global user.email "ArvindSah005@gmail.com"
 git help config/  git config help


git config --global --edit
git commit --amend --reset-author


=>update you local branch head 
git remote -v
git remote add origin <"clone">

=> to ignore some files for pushing the code-
- touch .gitignore // add the file names which needs to be excluded.




=> Making changes to project and comming them 

=> working with branchs
0. git branch <branch name> //Create your own branch 
0.1  git checkout <branch name> //switch to that branch 
0.2 git branch // will list all the local branch
      or 
git checkout -b NEW-BRANCH-NAME // to create and move to that branch
git branch -m OLD-BRANCH-NAME NEW-BRANCH-NAME // to rename the branch
git branch -d BRANCH-TO-DELETE // branch DELETE

   Merge A Branch into local master
1. git checkout master
2. git pull origin master // to check if there is any new changes into remote master branch
3. git branch --merged // this will give you the list of the all the branch which are merged local 
   master
4. git merge branch_Name
5. git push origin master // push your code into remote master branch
5. git push -u origin nweBranchName // push your code into remote new branch branch
// some testing 

   Delete a branch post merging into local master branch
- git branch --merged // this will the branch ed merged into local master
- git branch -d <branch_Name> // this will delete the local branch
- git branch -a // this will give the all the remote branchs
- git push origin --delete <remote_branch_Name> // this will delete the remote branch



   0.3 git status
1. git add . or git all -A
2. git commit -m 'comment'
   git pull origin master // origin is the name given to the remote repo url- check "git remote -v"
3. git push origin 'branch name'


=>remove the file which is been staged using git add
- git rm --cached fileName // to remove some specific file name
- git reset //to remove all the files from staging area which are put using git add command
- git reset <some file> //to remove specific file name from staging area
- git log // will give you the all the info post moving the changes to staging area
- ls -la// list of files with the time stamp
git remote -v // this will help you to get the remotely connected connection
git branch -a
git diff// view all the changes done
add <shortname> <URL> // this wil help you to add new remote connections
rename <old-name> <new-name> //Renames the remote connection.



Some other source links-
https://dev.to/juni/git-and-github---must-know-commands-to-make-your-first-commit-333c

